# Calculate Network Topology

Calculate Network Topology

## Usage

``` r
calculate_topology(g, output_dir = NULL, prefix = "Network")
```

## Arguments

- g:

  igraph object.

- output_dir:

  Directory to save topology files (optional).

- prefix:

  Prefix for output files.
