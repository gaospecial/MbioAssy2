# Fit Neutral Community Model

Fit Neutral Community Model

## Usage

``` r
fit_sncm(spp, pool = NULL, taxon = NULL)
```

## Arguments

- spp:

  Abundance matrix (rows are samples, columns are OTUs).

- pool:

  Metacommunity abundance (optional).

- taxon:

  Taxonomy information (optional).
